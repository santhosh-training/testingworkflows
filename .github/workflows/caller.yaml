name: Reusable Workflow user
  
on:
  workflow_dispatch
jobs:
  do-it:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo Hello, world-Looking Good11!

  build-called:
  
    uses: kharatramesh/testingworkflows/.github/workflows/build.yaml@master
    
  deploy-called:
  
    uses: kharatramesh/testingworkflows/.github/workflows/deploy.yml@master
    needs: build-called
